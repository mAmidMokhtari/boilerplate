## What

<!-- One or two sentences. Link the issue. -->

## Checklist

- [ ] Follows the page / service / model contract in `AGENTS.md`
- [ ] No `fetch` / React Query outside the allowed layers
- [ ] Loading, error and empty states covered
- [ ] Web: `t()` for every string, checked in `/fa`. Admin: `TEXTS` for every string
- [ ] `pnpm check` is green locally
